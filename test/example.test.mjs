import { expect } from "chai";
import { distance } from "../src/example.mjs";

describe("Example test fixture", () => {
  it("Calculate distance between Sourth Pole and North Pole", () => {
    expect(distance).prototype.between(19000, 20009);
  });
});
